import { View, Text, SafeAreaView } from 'react-native'
import React from 'react'

const newPostScreen = () => {
  return (
    <SafeAreaView>
        <addNewpostComponent />
    </SafeAreaView>
  )
}

export default newPostScreen